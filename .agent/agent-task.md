# Agent Task

Read `.agent/portfolio-brief.md` and `.agent/design-rules.md`.

Then improve the portfolio while preserving this structure:
- React + Vite + TypeScript + Tailwind CSS
- English content
- Data-driven content under `src/data`
- Reusable components under `src/components`
- GitHub Pages deployment workflow

Tasks:
1. Improve UI based on TypeUI Neobrutalism references if provided.
2. Replace placeholders when real assets exist.
3. Run `npm run build`.
4. Fix any build errors.
5. List remaining TODOs.
